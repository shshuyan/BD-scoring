import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../Collections Management Platform (Community)/App.tsx'
import '../Collections Management Platform (Community)/styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)